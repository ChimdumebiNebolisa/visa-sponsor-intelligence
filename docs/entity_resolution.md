# Entity resolution

Entity resolution will preserve the `raw employer name -> legal entity -> parent organization` identity chain. Ambiguous matches will remain unresolved or enter a review queue; fuzzy name similarity alone will never merge parents.
