# Data dictionary

The canonical data model and required fields are defined in `SPEC.md`. Dataset-specific types, null semantics, evidence classes, and provenance fields will be documented here as each ingestion phase is implemented.
